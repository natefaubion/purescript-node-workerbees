{
  "private": true,
  "type": "module",
  "scripts": {
    "test:bundle": "spago bundle --module Test.Worker --outfile output/Test.Worker.js --platform node",
    "test": "npm run test:bundle && spago test"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
